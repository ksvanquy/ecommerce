# Database Connection (PostgreSQL) - optional, falls back to in-memory store if not provided
DATABASE_URL=

# JWT Secret for authentication tokens
JWT_SECRET=

# Server Port
PORT=3000

# Client Vite Configuration
VITE_API_BASE_URL=
